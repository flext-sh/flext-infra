# Generated by flext_infra codegen for flext-infra — DO NOT EDIT
fail_fast: true
repos:
  - repo: local
    hooks:
    # AI Hub owns installation and rejects partial staging before invoking
    # the runner with --all-files. Never stash or automatically stage repairs.
    # Keep Git's candidate index and clear CI to execute every local gate.
    # Complete each repair sequence before rejecting changed content.

      - id: flext-pre-commit-candidate
        name: Validate complete commit candidate
        language: system
        entry: >-
          bash -eu -o pipefail -c 'unset MAKEFLAGS CI; git diff --quiet --ignore-submodules=none; test -z "$(git ls-files --others --exclude-standard)"; make gen; make fix; make fmt; make check; git diff --exit-code --ignore-submodules=none; test -z "$(git ls-files --others --exclude-standard)"'
        pass_filenames: false
        always_run: true
        stages: [pre-commit, pre-merge-commit]

      - id: flext-pre-push-candidate
        name: Validate published candidate
        language: system
        entry: >-
          bash -eu -o pipefail -c 'unset MAKEFLAGS CI; test -z "$(git status --porcelain --untracked-files=all --ignore-submodules=none)"; make audit; make check; make test; test -z "$(git status --porcelain --untracked-files=all --ignore-submodules=none)"'
        pass_filenames: false
        always_run: true
        stages: [pre-push]
