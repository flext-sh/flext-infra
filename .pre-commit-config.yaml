# Generated by flext_infra codegen for flext-infra — DO NOT EDIT
repos:
  - repo: local
    hooks:
      # Why: fast fixes run before each commit; expensive gates run before push.
      # Pre-push is local operator validation and runs every BLOCKING gate: CI=Y
      # is never injected here, because it skips the lint/format/pyrefly gates
      # that must block a push (9b604d43). make test never inherits that token
      # either (mro-v4p5 forbids pytest under CI=Y).
      # A step declaring gates_skip (mro-ucxgl) narrows CHECK_GATES for the hook
      # context only: local `make check` and the `docs` verb keep the full set.
      # WHAT never leaks in from the caller: git hooks inherit the invoking
      # environment, so `make work WHAT=land` reached these steps two ways and
      # any step without its own WHAT (e.g. `make gen`) rejected it as an
      # unsupported selector, making every lane unpushable. BOTH carriers are
      # cleared: the exported shell variable, and MAKEFLAGS, which the parent
      # make exports as ` -- WHAT=land` and a child make re-reads with
      # command-line precedence — so unsetting the variable alone leaves the
      # override in place (MAKEOVERRIDES is never exported and clearing it here
      # would change nothing). APPLY leaks the same way and
      # is cleared with them: `make work ... APPLY=Y` exports it, the hook's
      # read-only steps inherit it, and a read-only verb rejects the token
      # outright ("verb check is read-only and does not accept APPLY"), so a
      # landing lane could not push. Each step then states its selector: the
      # declared `what` when it has one, otherwise the verb's own declared
      # default — a hook validates everything, never a caller-narrowed subset.

      - id: flext-pre-commit-clean
        name: FLEXT pre-commit clean
        language: system
        entry: >-
          bash -eu -o pipefail -c
          'unset WHAT MAKEFLAGS APPLY; make clean APPLY=Y'
        pass_filenames: false
        always_run: true
        stages: [pre-commit]

      - id: flext-pre-commit-setup
        name: FLEXT pre-commit setup
        language: system
        entry: >-
          bash -eu -o pipefail -c
          'unset WHAT MAKEFLAGS APPLY; make setup APPLY=Y'
        pass_filenames: false
        always_run: true
        stages: [pre-commit]

      - id: flext-pre-commit-gen
        name: FLEXT pre-commit gen
        language: system
        entry: >-
          bash -eu -o pipefail -c
          'unset WHAT MAKEFLAGS APPLY; make gen APPLY=Y'
        pass_filenames: false
        always_run: true
        stages: [pre-commit]

      - id: flext-pre-commit-fmt
        name: FLEXT pre-commit fmt
        language: system
        entry: >-
          bash -eu -o pipefail -c
          'unset WHAT MAKEFLAGS APPLY; make fmt APPLY=Y'
        pass_filenames: false
        always_run: true
        stages: [pre-commit]

      - id: flext-pre-commit-fix
        name: FLEXT pre-commit fix
        language: system
        entry: >-
          bash -eu -o pipefail -c
          'unset WHAT MAKEFLAGS APPLY; make fix APPLY=Y'
        pass_filenames: false
        always_run: true
        stages: [pre-commit]

      - id: flext-pre-commit-check
        name: FLEXT pre-commit check
        language: system
        entry: >-
          bash -eu -o pipefail -c
          'unset WHAT MAKEFLAGS APPLY; CI=Y make check'
        pass_filenames: false
        always_run: true
        stages: [pre-commit]

      - id: flext-pre-commit-test
        name: FLEXT pre-commit test
        language: system
        entry: >-
          bash -eu -o pipefail -c
          'unset WHAT MAKEFLAGS APPLY; make test'
        pass_filenames: false
        always_run: true
        stages: [pre-commit]


      - id: flext-pre-push-clean
        name: FLEXT pre-push clean
        language: system
        entry: >-
          bash -eu -o pipefail -c
          'unset WHAT MAKEFLAGS APPLY; unset $(git rev-parse --local-env-vars); before=$$(git status --porcelain); test -z "$$before" || { printf "ERROR: pre-push requires a clean tracked tree\n" >&2; exit 2; }; make clean APPLY=Y; after=$$(git status --porcelain); test "$$after" = "$$before" || { printf "ERROR: pre-push gate mutated the tracked tree\n" >&2; exit 2; }'
        pass_filenames: false
        always_run: true
        stages: [pre-push]

      - id: flext-pre-push-setup
        name: FLEXT pre-push setup
        language: system
        entry: >-
          bash -eu -o pipefail -c
          'unset WHAT MAKEFLAGS APPLY; unset $(git rev-parse --local-env-vars); before=$$(git status --porcelain); test -z "$$before" || { printf "ERROR: pre-push requires a clean tracked tree\n" >&2; exit 2; }; make setup APPLY=Y; after=$$(git status --porcelain); test "$$after" = "$$before" || { printf "ERROR: pre-push gate mutated the tracked tree\n" >&2; exit 2; }'
        pass_filenames: false
        always_run: true
        stages: [pre-push]

      - id: flext-pre-push-gen
        name: FLEXT pre-push gen
        language: system
        entry: >-
          bash -eu -o pipefail -c
          'unset WHAT MAKEFLAGS APPLY; unset $(git rev-parse --local-env-vars); before=$$(git status --porcelain); test -z "$$before" || { printf "ERROR: pre-push requires a clean tracked tree\n" >&2; exit 2; }; make gen APPLY=Y; after=$$(git status --porcelain); test "$$after" = "$$before" || { printf "ERROR: pre-push gate mutated the tracked tree\n" >&2; exit 2; }'
        pass_filenames: false
        always_run: true
        stages: [pre-push]

      - id: flext-pre-push-fmt
        name: FLEXT pre-push fmt
        language: system
        entry: >-
          bash -eu -o pipefail -c
          'unset WHAT MAKEFLAGS APPLY; unset $(git rev-parse --local-env-vars); before=$$(git status --porcelain); test -z "$$before" || { printf "ERROR: pre-push requires a clean tracked tree\n" >&2; exit 2; }; make fmt APPLY=Y; after=$$(git status --porcelain); test "$$after" = "$$before" || { printf "ERROR: pre-push gate mutated the tracked tree\n" >&2; exit 2; }'
        pass_filenames: false
        always_run: true
        stages: [pre-push]

      - id: flext-pre-push-fix
        name: FLEXT pre-push fix
        language: system
        entry: >-
          bash -eu -o pipefail -c
          'unset WHAT MAKEFLAGS APPLY; unset $(git rev-parse --local-env-vars); before=$$(git status --porcelain); test -z "$$before" || { printf "ERROR: pre-push requires a clean tracked tree\n" >&2; exit 2; }; make fix APPLY=Y; after=$$(git status --porcelain); test "$$after" = "$$before" || { printf "ERROR: pre-push gate mutated the tracked tree\n" >&2; exit 2; }'
        pass_filenames: false
        always_run: true
        stages: [pre-push]

      - id: flext-pre-push-check
        name: FLEXT pre-push check
        language: system
        entry: >-
          bash -eu -o pipefail -c
          'unset WHAT MAKEFLAGS APPLY; unset $(git rev-parse --local-env-vars); before=$$(git status --porcelain); test -z "$$before" || { printf "ERROR: pre-push requires a clean tracked tree\n" >&2; exit 2; }; make check; after=$$(git status --porcelain); test "$$after" = "$$before" || { printf "ERROR: pre-push gate mutated the tracked tree\n" >&2; exit 2; }'
        pass_filenames: false
        always_run: true
        stages: [pre-push]

      - id: flext-pre-push-test
        name: FLEXT pre-push test
        language: system
        entry: >-
          bash -eu -o pipefail -c
          'unset WHAT MAKEFLAGS APPLY; unset $(git rev-parse --local-env-vars); before=$$(git status --porcelain); test -z "$$before" || { printf "ERROR: pre-push requires a clean tracked tree\n" >&2; exit 2; }; make test; after=$$(git status --porcelain); test "$$after" = "$$before" || { printf "ERROR: pre-push gate mutated the tracked tree\n" >&2; exit 2; }'
        pass_filenames: false
        always_run: true
        stages: [pre-push]

