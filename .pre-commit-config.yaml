# Generated by flext_infra codegen for flext-infra — DO NOT EDIT
repos:
  - repo: local
    hooks:
      # Why: each make verb is its own pre-commit/pre-push hook so the operator
      # sees per-step status (Passed/Failed) natively via the pre-commit runner.
      # Every step states the CI token instead of inheriting it. A git hook runs
      # with the invoking environment, so a caller that already exported
      # CI=Y would otherwise reach the pre-push steps and
      # revoke exactly the gates a push must not skip (9b604d43: lint, format,
      # pyrefly; mro-v4p5: pytest). Declaring the token closes that carrier.
      # pre-commit is the fast mutation pass and runs the CI gate set
      # (CI=Y). pre-push is the local blocking gate and runs
      # the full set with CI=N, the local arm of the same
      # ternary: only the exact value Y disables coverage and
      # narrows CHECK_GATES, so N keeps every gate and the
      # complete pytest+coverage run.
      # Hook isolation and the local extension suffix are business policy from
      # config/codegen.yaml. After the managed command succeeds, an executable
      # hooks/<generated-hook-id>.local is invoked when present. That sibling is the
      # ungenerated, repository-local customization surface.
      # A step declaring gates_skip (mro-ucxgl) narrows CHECK_GATES for the hook
      # context only: local `make check` and the `docs` verb keep the full set.



      - id: flext-pre-commit-setup
        name: make setup
        language: system
        entry: >-
          bash -eu -o pipefail -c 'unset WHAT MAKEFLAGS APPLY; CI=Y make setup; local_hook="$(git rev-parse --git-path hooks/flext-pre-commit-setup.local)"; [ ! -x "$local_hook" ] || "$local_hook"'
        pass_filenames: false
        always_run: true
        stages: [pre-commit]

      - id: flext-pre-commit-gen
        name: make gen APPLY=Y
        language: system
        entry: >-
          bash -eu -o pipefail -c 'unset WHAT MAKEFLAGS APPLY; CI=Y make gen APPLY=Y; local_hook="$(git rev-parse --git-path hooks/flext-pre-commit-gen.local)"; [ ! -x "$local_hook" ] || "$local_hook"'
        pass_filenames: false
        always_run: true
        stages: [pre-commit]

      - id: flext-pre-commit-fmt
        name: make fmt APPLY=Y
        language: system
        entry: >-
          bash -eu -o pipefail -c 'unset WHAT MAKEFLAGS APPLY; CI=Y make fmt APPLY=Y; local_hook="$(git rev-parse --git-path hooks/flext-pre-commit-fmt.local)"; [ ! -x "$local_hook" ] || "$local_hook"'
        pass_filenames: false
        always_run: true
        stages: [pre-commit]

      - id: flext-pre-commit-fix
        name: make fix APPLY=Y
        language: system
        entry: >-
          bash -eu -o pipefail -c 'unset WHAT MAKEFLAGS APPLY; CI=Y make fix APPLY=Y; local_hook="$(git rev-parse --git-path hooks/flext-pre-commit-fix.local)"; [ ! -x "$local_hook" ] || "$local_hook"'
        pass_filenames: false
        always_run: true
        stages: [pre-commit]

      - id: flext-pre-commit-check
        name: make check
        language: system
        entry: >-
          bash -eu -o pipefail -c 'unset WHAT MAKEFLAGS APPLY; CI=Y make check; local_hook="$(git rev-parse --git-path hooks/flext-pre-commit-check.local)"; [ ! -x "$local_hook" ] || "$local_hook"'
        pass_filenames: false
        always_run: true
        stages: [pre-commit]



      - id: flext-pre-push-setup
        name: make setup
        language: system
        entry: >-
          bash -eu -o pipefail -c 'unset WHAT MAKEFLAGS APPLY; unset $(git rev-parse --local-env-vars); CI=N make setup; local_hook="$(git rev-parse --git-path hooks/flext-pre-push-setup.local)"; [ ! -x "$local_hook" ] || "$local_hook"'
        pass_filenames: false
        always_run: true
        stages: [pre-push]

      - id: flext-pre-push-gen
        name: make gen APPLY=Y
        language: system
        entry: >-
          bash -eu -o pipefail -c 'unset WHAT MAKEFLAGS APPLY; unset $(git rev-parse --local-env-vars); CI=N make gen APPLY=Y; local_hook="$(git rev-parse --git-path hooks/flext-pre-push-gen.local)"; [ ! -x "$local_hook" ] || "$local_hook"'
        pass_filenames: false
        always_run: true
        stages: [pre-push]

      - id: flext-pre-push-fmt
        name: make fmt APPLY=Y
        language: system
        entry: >-
          bash -eu -o pipefail -c 'unset WHAT MAKEFLAGS APPLY; unset $(git rev-parse --local-env-vars); CI=N make fmt APPLY=Y; local_hook="$(git rev-parse --git-path hooks/flext-pre-push-fmt.local)"; [ ! -x "$local_hook" ] || "$local_hook"'
        pass_filenames: false
        always_run: true
        stages: [pre-push]

      - id: flext-pre-push-fix
        name: make fix APPLY=Y
        language: system
        entry: >-
          bash -eu -o pipefail -c 'unset WHAT MAKEFLAGS APPLY; unset $(git rev-parse --local-env-vars); CI=N make fix APPLY=Y; local_hook="$(git rev-parse --git-path hooks/flext-pre-push-fix.local)"; [ ! -x "$local_hook" ] || "$local_hook"'
        pass_filenames: false
        always_run: true
        stages: [pre-push]

      - id: flext-pre-push-check
        name: make check
        language: system
        entry: >-
          bash -eu -o pipefail -c 'unset WHAT MAKEFLAGS APPLY; unset $(git rev-parse --local-env-vars); CI=N make check; local_hook="$(git rev-parse --git-path hooks/flext-pre-push-check.local)"; [ ! -x "$local_hook" ] || "$local_hook"'
        pass_filenames: false
        always_run: true
        stages: [pre-push]

      - id: flext-pre-push-test-cache-clear
        name: make test WHAT=cache-clear APPLY=Y
        language: system
        entry: >-
          bash -eu -o pipefail -c 'unset WHAT MAKEFLAGS APPLY; unset $(git rev-parse --local-env-vars); CI=N make test WHAT=cache-clear APPLY=Y; local_hook="$(git rev-parse --git-path hooks/flext-pre-push-test-cache-clear.local)"; [ ! -x "$local_hook" ] || "$local_hook"'
        pass_filenames: false
        always_run: true
        stages: [pre-push]

      - id: flext-pre-push-test-full
        name: make test WHAT=full
        language: system
        entry: >-
          bash -eu -o pipefail -c 'unset WHAT MAKEFLAGS APPLY; unset $(git rev-parse --local-env-vars); CI=N make test WHAT=full; local_hook="$(git rev-parse --git-path hooks/flext-pre-push-test-full.local)"; [ ! -x "$local_hook" ] || "$local_hook"'
        pass_filenames: false
        always_run: true
        stages: [pre-push]

