{# mro-wkii.17.26 (codex): tool pins render only from validated config. -#}
# @generated by: flext_infra workspace sync

[settings]
python.default_packages_file = ".default-python-packages"

[tools]
python = "{{ python_version }}"
uv = "{{ uv_version }}"
ruff = "{{ ruff_version }}"
