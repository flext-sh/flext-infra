{# Tool selectors render only from validated config. -#}
# @generated by: flext_infra workspace sync
# mise carries only the compatible Python line. uv and quality tools come from
# the project environment and are invoked through the generated Make surface.

[tools]
python = "{{ python_version }}"
