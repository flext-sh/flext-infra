{# Tool selectors render only from validated config. -#}
# @generated by: flext_infra workspace sync
# mise carries only the Python runtime. Linters and uv come from the project
# environment and are invoked through the generated Make surface.

[tools]
python = "{{ python_version }}"
