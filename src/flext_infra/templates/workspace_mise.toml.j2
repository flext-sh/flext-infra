{# mro-wkii.17.26 (codex): tool pins render only from validated config. -#}
# @generated by: flext_infra workspace sync
# mise carries ONLY language runtimes (python, uv) as exact fleet-wide pins
# from the config SSOT. Linters/type-checkers come from the locked dependency
# groups (pyproject dev group) into the workspace .venv; gates invoke them via
# python -m, so mise never pins linter tools.

[tools]
python = "{{ python_version }}"
uv = "{{ uv_version }}"
