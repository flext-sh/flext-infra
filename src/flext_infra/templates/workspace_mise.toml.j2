{# mro-wkii.17.26 (codex): tool pins render only from validated config. -#}
# @generated by: flext_infra workspace sync
# mise resolves compatible Python and uv lines. Linters come from the locked
# project environment and are invoked through the generated Make surface.

[tools]
python = "{{ python_mise_version }}"
uv = "{{ uv_mise_version }}"
