# Generated by flext_infra codegen — DO NOT EDIT.
# Release build policy: the isolated `uv build --build-constraints … --require-hashes`
# of every release artifact resolves its build backend from exactly these pins.
# Owner: flext-infra config/infra.yaml (release.build_constraints); regenerate with `make gen APPLY=Y`.
{%- for pin in build_constraints %}
{{ pin.name }}=={{ pin.version }} \
{%- for digest in pin.hashes %}
    --hash=sha256:{{ digest }}{{ " \\" if not loop.last else "" }}
{%- endfor %}
{%- endfor %}
