# Generated by flext_infra codegen — DO NOT EDIT.
# Pre-commit only orchestrates the canonical Make lifecycle; tools remain owned
# by their single Make verb.
repos:
  - repo: local
    hooks:
{% for verb in make.lifecycle_verbs %}
      - id: flext-{{ verb.name }}
        name: FLEXT canonical {{ verb.name }}
        entry: make {{ verb.name }}{% if verb.apply_guarded %} APPLY={{ make.apply_value }}{% endif %}
        language: system
        pass_filenames: false
        always_run: true
{% endfor %}
