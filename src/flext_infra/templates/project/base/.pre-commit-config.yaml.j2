# Generated by flext_infra codegen for {{ dist }} — DO NOT EDIT
repos:
  - repo: local
    hooks:
      - id: flext-canonical-workflow
        name: FLEXT canonical workflow
        language: system
        entry: >-
          bash -eu -o pipefail -c
          '{% for step in make.workflow.hook %}make {{ step.verb }}{% if step.apply %} {{ make.apply_variable }}={{ make.apply_value }}{% endif %}{% if not loop.last %} && {% endif %}{% endfor %}'
        pass_filenames: false
        always_run: true
