# @flext-generated: continuous
# @flext-owner: flext-infra/config/codegen.yaml toolchain.beads + config/beads.yaml
# @flext-adjust: edit toolchain.beads or workspace.yaml gascity_enabled; never this file
# @flext-regenerate: make gen APPLY=Y
# Generated by `flext-infra gen conform`.
# Source: repository-local identity{{ " plus the Gas City contract in config/codegen.yaml" if gascity_enabled else "" }}.
# Repository-local projection; conform only renders configuration and never
# initializes, starts, or verifies runtime state.
# bd reads `issue_prefix` (underscore). Do not emit `issue-prefix`.
issue_prefix: {{ issue_prefix | tojson }}
dolt.auto-start: {{ "false" if gascity_enabled else "true" }}
export.auto: {{ export_auto | tojson }}
backup.enabled: {{ backup_enabled | tojson }}
{%- if gascity_enabled %}
gc.endpoint_origin: {{ endpoint_origin }}
gc.endpoint_status: {{ endpoint_status }}
{%- endif %}
types.custom: {{ custom_issue_types | join(",") }}
dolt:
  mode: {{ dolt_mode }}
  disable-event-flush: {{ dolt_disable_event_flush | tojson }}
