"""Integration-test case for {{ package_name }} — one ready MRO case per module.

The single class below inherits ``FlextTestsCase`` so every integration test gets
``service``/``settings``/``logger`` plus the canonical ``c/t/p/m/u`` facades for
free (no per-test settings/runtime wiring). Concrete cases subclass it and only
write assertions (operator, mro-wkii.13; rule: one nested class per module).

Copyright (c) {{ year }} {{ author_name }}. All rights reserved.
SPDX-License-Identifier: MIT
"""

from __future__ import annotations

from flext_tests import FlextTestsCase


class Tests{{ class_stem }}IntegrationCase(FlextTestsCase):
    """Ready MRO case for {{ class_stem }} integration tests (one class/module)."""


__all__: list[str] = ["Tests{{ class_stem }}IntegrationCase"]
