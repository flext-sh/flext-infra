"""End-to-end test case for {{ package_name }} — one ready MRO case per module.

The single class below inherits ``FlextTestsCase`` so every e2e test gets
``service``/``settings``/``logger`` plus the canonical ``c/t/p/m/u`` facades for
free (no per-test settings/runtime wiring). Concrete cases subclass it and only
write assertions against the PUBLIC entrypoint (the generated CLI / facade),
never against private structure. One nested class per module.

Copyright (c) {{ year }} {{ author_name }}. All rights reserved.
SPDX-License-Identifier: MIT
"""

from __future__ import annotations

from flext_tests import FlextTestsCase


class Tests{{ class_stem }}E2ECase(FlextTestsCase):
    """Ready MRO case for {{ class_stem }} end-to-end tests (one class/module)."""


__all__: list[str] = ["Tests{{ class_stem }}E2ECase"]
