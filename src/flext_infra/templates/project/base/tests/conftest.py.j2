"""Conftests for {{ package_name }} tests.

Copyright (c) {{ year }} {{ author_name }}. All rights reserved.
SPDX-License-Identifier: MIT
"""

from __future__ import annotations{# NOTE (multi-agent, mro-wkii.19.1): pytest consumes the shared fixture plugin directly; no local settings aliases. #}

# Unified pytest-config SSOT: surface the shared fixtures package so every
# unit/e2e/integration test reaches typed fixtures without per-directory
# conftest duplication. Real fixtures only (no mock/fake/patch).
pytest_plugins = ["tests.fixtures"]
