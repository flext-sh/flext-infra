"""Shared test fixtures for {{ package_name }}.

Reusable, typed fixtures live here and are surfaced through the unified
``tests/conftest.py`` (single pytest-config SSOT). Fixtures return real domain
models built via the canonical ``m.{{ ns }}.*`` facade — never mocks, fakes, or
patches — so tests exercise genuine behavior over the public interface.

Copyright (c) {{ year }} {{ author_name }}. All rights reserved.
SPDX-License-Identifier: MIT
"""

from __future__ import annotations

import pytest

from {{ package_name }} import m


@pytest.fixture
def ping_input() -> m.{{ ns }}.PingInput:
    """A real ``ping`` input model built through the public ``m`` facade."""
    return m.{{ ns }}.PingInput()


__all__: list[str] = ["ping_input"]
