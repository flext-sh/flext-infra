# @generated by: flext_infra codegen new
# NOTE(mro-wkii.14, agent codegen): workspace root PAI dos clones em
# .flext-deps/* (PROVADO por uv lock: sem este arquivo, uv falha com
# "references a workspace in tool.uv.sources but is not a workspace member" —
# os flext-* clonados carregam sources workspace=true internos que só resolvem
# sob um workspace root PAI). Este arquivo é COMMITADO (exceção no .gitignore);
# `flext-infra deps internal-sync` clona os subdirs ao lado dele. O projeto
# raiz consome os members via path editable (sources em <raiz>/pyproject.toml)
# SEM ser member. members/sources = plataforma (fixos).

[project]
name = "{{ package_name }}-deps"
version = "0.0.0"
requires-python = "{{ python_requires }}"

[tool.uv.workspace]
members = ["flext-cli", "flext-core", "flext-infra", "flext-tests"]

[tool.uv.sources.flext-cli]
workspace = true

[tool.uv.sources.flext-core]
workspace = true

[tool.uv.sources.flext-infra]
workspace = true

[tool.uv.sources.flext-tests]
workspace = true
