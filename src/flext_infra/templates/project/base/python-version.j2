{# Supported Python family. The whitespace-control marker below strips this
   comment's newline: .python-version is read as a bare version string, so a
   leading blank line makes the generated file drift forever. -#}
{{ python_version }}
