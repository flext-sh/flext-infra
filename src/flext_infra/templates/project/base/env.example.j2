# Environment overrides for {{ dist }} (prefix {{ env_prefix }}).
# Copy to .env and fill real values; .env is gitignored — never commit secrets.
{{ env_prefix }}{{ ns | upper }}__ENABLED={% if scaffold.ping_example.enabled_default %}true{% else %}false{% endif %}
