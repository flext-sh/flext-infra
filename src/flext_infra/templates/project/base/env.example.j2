# Environment overrides for {{ dist }} (prefix {{ repository_env_prefix }}).
# Copy to .env and fill real values; .env is gitignored — never commit secrets.
{{ repository_env_prefix }}ENABLED={% if scaffold.ping_example.enabled_default %}true{% else %}false{% endif %}
