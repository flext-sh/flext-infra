# Private project handlers for {{ dist }}.
# NOTE (multi-agent, mro-wkii.17.4 / agent: codex): this versioned extension
# accepts only `_custom_<verb>_<what>` handlers. Public targets, aliases,
# toolchain setup, generated-target redefinitions, and help entries are invalid.
