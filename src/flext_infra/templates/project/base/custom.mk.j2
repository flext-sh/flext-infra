# Private lifecycle hooks for {{ dist }}.
# Define only pre-<verb> or post-<verb> for an operation declared by the typed
# Make catalog. A distinct operation is a named public verb, never a parameter.
