"""{{ class_stem }} utilities — extends the upstream ``u`` facade (pure helpers).

Project-specific helpers live under ``u.{{ ns }}.*``; everything else is inherited
from the upstream facade. The ``{{ ns }}`` namespace is composed from the
``_utilities/{{ ns_attr }}.py`` domain part via MRO.

Copyright (c) {{ year }} {{ author_name }}. All rights reserved.
SPDX-License-Identifier: MIT
"""

from __future__ import annotations

from {{ upstream }} import u

from {{ package_name }}._utilities.{{ ns_attr }} import {{ class_stem }}Utilities{{ ns }}


class {{ class_stem }}Utilities(u):
    """{{ class_stem }} utility facade extending the upstream ``u`` facade."""

    class {{ ns }}({{ class_stem }}Utilities{{ ns }}):
        """Project-specific pure helpers namespace (composed via MRO)."""


u = {{ class_stem }}Utilities

__all__: list[str] = ["{{ class_stem }}Utilities", "u"]
