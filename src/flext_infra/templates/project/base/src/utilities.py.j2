"""{{ class_stem }} utilities — extends the upstream ``u`` facade (pure helpers).

Copyright (c) {{ year }} {{ author_name }}. All rights reserved.
SPDX-License-Identifier: MIT
"""

from __future__ import annotations

from {{ upstream }} import u

from {{ package_name }}._utilities.config import {{ class_stem }}UtilitiesConfig
from {{ package_name }}._utilities.settings import {{ class_stem }}UtilitiesSettings


class {{ class_stem }}Utilities(u):
    """{{ class_stem }} utility facade extending the upstream ``u`` facade.

    Access patterns:
    - ``u.{{ ns }}.*`` — project-specific pure helpers (config/settings via MRO).
    - ``u.*`` — inherited from the upstream facade (do NOT override).
    """

    class {{ ns }}(
        {{ class_stem }}UtilitiesConfig,
        {{ class_stem }}UtilitiesSettings,
    ):
        """Project-specific pure helpers, composed from config/settings parts via MRO."""


# NOTE (multi-agent, mro-wkii.14 / agent: codegen): external facade single-base
# (alias ``u``) + inner class ``{{ ns }}`` composing *Config/*Settings via MRO
# (cosmos-main pattern); satisfies ENFORCE-047/049 (AGENTS.md U2-U8).
u = {{ class_stem }}Utilities

__all__: list[str] = ["{{ class_stem }}Utilities", "u"]
