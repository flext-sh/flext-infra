"""{{ class_stem }} type definitions — extends the upstream ``t`` facade.

Project-specific type aliases live under ``t.{{ ns }}.*``; everything else is
inherited from the upstream facade. The ``{{ ns }}`` namespace is composed from
the ``_typings/{{ ns_attr }}.py`` domain part via MRO.

Copyright (c) {{ year }} {{ author_name }}. All rights reserved.
SPDX-License-Identifier: MIT
"""

from __future__ import annotations

from {{ upstream }} import t

from {{ package_name }}._typings.{{ ns_attr }} import {{ class_stem }}Types{{ ns }}


class {{ class_stem }}Types(t):
    """{{ class_stem }} type namespace extending the upstream ``t`` facade."""

    class {{ ns }}({{ class_stem }}Types{{ ns }}):
        """Project-specific type aliases namespace (composed via MRO)."""


t = {{ class_stem }}Types

__all__: list[str] = ["{{ class_stem }}Types", "t"]
