"""Structural config declarations for {{ package_name }}.

Copyright (c) {{ year }} {{ author_name }}. All rights reserved.
SPDX-License-Identifier: MIT
"""

{# NOTE (multi-agent, mro-wkii.19.1): field-only protocols describe direct singleton data without forwarding properties. #}
from __future__ import annotations

from typing import Final, Protocol, runtime_checkable


class {{ class_stem }}ProtocolsConfig:
    """Protocol namespace for validated config data."""

    @runtime_checkable
    class ConfigNamespace(Protocol):
        """Validated ``config.{{ ns }}`` fields."""

        name: Final[str]
        version: Final[str]
        greeting: Final[str]
        ping_reply: Final[str]
        disabled_reply: Final[str]

    @runtime_checkable
    class Config(Protocol):
        """Direct project config singleton surface."""

        {{ ns }}: Final[{{ class_stem }}ProtocolsConfig.ConfigNamespace]


__all__: list[str] = ["{{ class_stem }}ProtocolsConfig"]
