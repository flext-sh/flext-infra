{# NOTE (codex, mro-j47u): precompute compact/multiline Protocol declarations from line length. -#}
{%- set config_property_line = "        def " ~ ns ~ "(self) -> m." ~ ns ~ ".ConfigRoot.ConfigNamespace:" -%}
{%- set config_property_declaration = config_property_line if config_property_line | length <= tooling.tools.ruff.line_length else "        def " ~ ns ~ "(\n            self,\n        ) -> m." ~ ns ~ ".ConfigRoot.ConfigNamespace:" -%}
"""Structural config declarations for {{ package_name }}.

Copyright (c) {{ year }} {{ author_name }}. All rights reserved.
SPDX-License-Identifier: MIT
"""

{# NOTE (multi-agent, mro-wkii.19.1): field-only protocols describe direct singleton data without forwarding properties. -#}
from __future__ import annotations

from typing import TYPE_CHECKING, Protocol, runtime_checkable

if TYPE_CHECKING:
    from {{ package_name }}.models import m


class {{ class_stem }}ProtocolsConfig:
    """Protocol namespace for validated config data."""

    @runtime_checkable
    class Config(Protocol):
        """Direct project config singleton surface."""

        @property
{{ config_property_declaration }}
            """The canonical validated config namespace model."""
            ...


__all__: list[str] = ["{{ class_stem }}ProtocolsConfig"]
