{# NOTE (codex, mro-j47u): precompute compact/multiline Protocol declarations from line length. -#}
{%- set settings_property_line = "        def " ~ ns ~ "(self) -> m." ~ ns ~ ".SettingsRoot.SettingsNamespace:" -%}
{%- set settings_property_declaration = settings_property_line if settings_property_line | length <= tooling.tools.ruff.line_length else "        def " ~ ns ~ "(\n            self,\n        ) -> m." ~ ns ~ ".SettingsRoot.SettingsNamespace:" -%}
"""Structural settings declarations for {{ package_name }}.

Copyright (c) {{ year }} {{ author_name }}. All rights reserved.
SPDX-License-Identifier: MIT
"""

{# NOTE (multi-agent, mro-wkii.19.1): field-only protocols describe direct singleton data without forwarding properties. -#}
from __future__ import annotations

from typing import TYPE_CHECKING, Protocol, runtime_checkable

if TYPE_CHECKING:
    from {{ package_name }}.models import m


class {{ class_stem }}ProtocolsSettings:
    """Protocol namespace for validated settings data."""

    @runtime_checkable
    class Settings(Protocol):
        """Direct project settings singleton surface."""

        @property
{{ settings_property_declaration }}
            """The canonical validated settings namespace model."""
            ...


__all__: list[str] = ["{{ class_stem }}ProtocolsSettings"]
