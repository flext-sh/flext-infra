"""Structural settings declarations for {{ package_name }}.

Copyright (c) {{ year }} {{ author_name }}. All rights reserved.
SPDX-License-Identifier: MIT
"""

{# NOTE (multi-agent, mro-wkii.19.1): field-only protocols describe direct singleton data without forwarding properties. #}
from __future__ import annotations

from typing import Final, Protocol, runtime_checkable


class {{ class_stem }}ProtocolsSettings:
    """Protocol namespace for validated settings data."""

    @runtime_checkable
    class SettingsNamespace(Protocol):
        """Validated ``settings.{{ ns }}`` fields."""

        enabled: Final[bool]

    @runtime_checkable
    class Settings(Protocol):
        """Direct project settings singleton surface."""

        {{ ns }}: Final[{{ class_stem }}ProtocolsSettings.SettingsNamespace]


__all__: list[str] = ["{{ class_stem }}ProtocolsSettings"]
