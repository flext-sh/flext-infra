"""Structural ping declarations for {{ package_name }}.

Copyright (c) {{ year }} {{ author_name }}. All rights reserved.
SPDX-License-Identifier: MIT
"""

{# NOTE (multi-agent, mro-wkii.19.1): public ping annotations use field-only protocols; Pydantic models only validate and construct. #}
from __future__ import annotations

from typing import Final, Protocol, runtime_checkable


class {{ class_stem }}ProtocolsPing:
    """Protocol namespace for the public ping contract."""

    @runtime_checkable
    class PingInput(Protocol):
        """Structural marker for the fieldless ping request."""

    @runtime_checkable
    class PingResult(Protocol):
        """Structural surface of a successful ping response."""

        message: Final[str]


__all__: list[str] = ["{{ class_stem }}ProtocolsPing"]
