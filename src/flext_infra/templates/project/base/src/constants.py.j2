"""{{ class_stem }} constants — extends the upstream ``c`` facade.

Project-specific constants live under ``c.{{ ns }}.*``; everything else is
inherited from the upstream facade. The ``{{ ns }}`` namespace is composed from
the ``_constants/{{ ns_attr }}.py`` domain part via MRO.

Copyright (c) {{ year }} {{ author_name }}. All rights reserved.
SPDX-License-Identifier: MIT
"""

from __future__ import annotations

from {{ upstream }} import c

from {{ package_name }}._constants.{{ ns_attr }} import {{ class_stem }}Constants{{ ns }}


class {{ class_stem }}Constants(c, {{ class_stem }}Constants{{ ns }}):
    """{{ class_stem }} domain constants extending the upstream ``c`` facade."""

    class {{ ns }}({{ class_stem }}Constants{{ ns }}):
        """Project-specific constants namespace (composed via MRO)."""


c = {{ class_stem }}Constants

__all__: list[str] = ["{{ class_stem }}Constants", "c"]
