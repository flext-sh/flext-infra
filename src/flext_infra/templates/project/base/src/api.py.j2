"""Public API facade for {{ package_name }}.

The package root freezes this facade as the stable user entrypoint. Consumers
import ``{{ class_stem }}`` or ``{{ alias }}`` from ``{{ package_name }}`` and
reach domain concerns through canonical aliases and namespaces.

Copyright (c) {{ year }} {{ author_name }}. All rights reserved.
SPDX-License-Identifier: MIT
"""

from __future__ import annotations{# NOTE (codex, mro-j47u): the facade composes services, never base behavior. #}

from {{ package_name }}.services.ping import {{ class_stem }}Ping


class {{ class_stem }}({{ class_stem }}Ping):
    """Public {{ class_stem }} facade composed through cooperative MRO."""


{{ alias }} = {{ class_stem }}.fetch_global()


__all__: list[str] = ["{{ class_stem }}", "{{ alias }}"]
