"""{{ class_stem }} settings-domain constants part (composed into ``c.{{ class_stem }}`` via MRO).

Pure module: imports nothing from the project and nothing from flext, so it can
be composed by ``c``/``t``/``p``/``m``/``u`` without creating an import cycle.
Domain *keys* live here (env prefix etc.), never business-rule values.

Copyright (c) {{ year }} {{ author_name }}. All rights reserved.
SPDX-License-Identifier: MIT
"""

from __future__ import annotations
{# mro-wkii.14 (agent: codegen) — cosmos-main / AGENTS.md U2-U8 pattern (pure leaf; ENV_PREFIX as a key; settings mirrors config). #}
from typing import Final


class {{ class_stem }}ConstantsSettings:
    """Settings-domain constants for {{ class_stem }} (env prefix etc.; pure)."""

    ENV_PREFIX: Final[str] = "{{ env_prefix }}"


__all__: list[str] = ["{{ class_stem }}ConstantsSettings"]
