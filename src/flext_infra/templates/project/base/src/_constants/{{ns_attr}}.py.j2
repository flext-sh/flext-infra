"""{{ ns }} constants namespace (pure declaration; composed into ``c.{{ ns }}`` via MRO).

Pure module: only ``Final`` scalars and ``StrEnum``/``IntEnum`` members — no
methods, no helpers. The ``constants.py`` facade composes this class into the
``{{ ns }}`` namespace by MRO.

Copyright (c) {{ year }} {{ author_name }}. All rights reserved.
SPDX-License-Identifier: MIT
"""

from __future__ import annotations

from enum import StrEnum
from typing import Final


class {{ class_stem }}Constants{{ ns }}:
    """{{ ns }} domain constants (project-specific keys and command names)."""

    NAME: Final[str] = "{{ const_name }}"
    VERSION: Final[str] = "{{ version }}"

    class CommandType(StrEnum):
        """CLI command names for {{ package_name }} (str-compatible)."""

        PING = "ping"


__all__: list[str] = ["{{ class_stem }}Constants{{ ns }}"]
