"""{{ class_stem }} config-domain constants part (composed into ``c.{{ class_stem }}`` via MRO).

Pure module: imports nothing from the project and nothing from flext, so it can
be composed by ``c``/``t``/``p``/``m``/``u`` without creating an import cycle.
Domain *keys* live here (never business-rule values — those stay in
``config/*.yaml``).

Copyright (c) {{ year }} {{ author_name }}. All rights reserved.
SPDX-License-Identifier: MIT
"""

from __future__ import annotations
{# mro-wkii.14 (agent: codegen) — cosmos-main / AGENTS.md U2-U8 pattern (pure leaf, no facade import; keys, never business-rule values). #}
from typing import Final


class {{ class_stem }}ConstantsConfig:
    """Config-domain key constants for {{ class_stem }} (no business rules)."""

    CONFIG_DIR: Final[str] = "config"


__all__: list[str] = ["{{ class_stem }}ConstantsConfig"]
