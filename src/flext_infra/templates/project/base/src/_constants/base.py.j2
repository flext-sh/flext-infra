"""{{ ns }} constants base part (composed into ``c.{{ ns }}`` via MRO).

Pure module: only ``Final`` scalars and ``StrEnum`` members. Project metadata is
read from the already-loaded config singleton and is never hardcoded twice.

Copyright (c) {{ year }} {{ author_name }}. All rights reserved.
SPDX-License-Identifier: MIT
"""

from __future__ import annotations

from enum import StrEnum
from typing import Final

from {{ package_name }} import config


class {{ class_stem }}ConstantsBase:
    """{{ ns }} core constants (project-specific keys and command names)."""

    NAME: Final[str] = config.{{ ns }}.name
    VERSION: Final[str] = config.{{ ns }}.version

    class CommandType(StrEnum):
        """CLI command names for {{ package_name }} (str-compatible)."""

        PING = "ping"


__all__: list[str] = ["{{ class_stem }}ConstantsBase"]
