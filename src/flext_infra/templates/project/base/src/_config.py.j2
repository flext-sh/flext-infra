{# NOTE (multi-agent, mro-wkii.19.1): the private loader composes the pure model and exports only the direct singleton. -#}
"""Direct {{ class_stem }} config singleton export.

Copyright (c) {{ year }} {{ author_name }}. All rights reserved.
SPDX-License-Identifier: MIT
"""

from __future__ import annotations

from pathlib import Path
from typing import TYPE_CHECKING, ClassVar

from {{ package_name }}._models.config import {{ class_stem }}ConfigModels
from flext_core import FlextConfig

if TYPE_CHECKING:
    from {{ package_name }}._protocols.config import {{ class_stem }}ProtocolsConfig


class _{{ class_stem }}Config(FlextConfig, {{ class_stem }}ConfigModels.ConfigRoot):
    """Private source loader composed with the validated config root."""

    CONFIG_DIR: ClassVar[str] = str(Path(__file__).resolve().parents[2] / "config")


config: {{ class_stem }}ProtocolsConfig.Config = _{{ class_stem }}Config()

__all__: list[str] = ["config"]
