"""{{ class_stem }}Config — frozen config singleton for {{ package_name }} (ADR-005).

Model-less: business rules live in ``config/*.yaml`` under the ``{{ ns }}:`` key
and are exposed through the open ``config.{{ ns }}`` namespace (``extra="allow"``,
frozen). Access is ``config.{{ ns }}.<domain>``. This is a pure declaration
module: no methods, no helpers — only the namespace model and the singleton.

Copyright (c) {{ year }} {{ author_name }}. All rights reserved.
SPDX-License-Identifier: MIT
"""

from __future__ import annotations

from pydantic import BaseModel, ConfigDict

from flext_core import FlextConfig


class _{{ ns }}Namespace(BaseModel):
    """Open, frozen namespace exposing every ``config/*.yaml`` domain model-less."""

    model_config = ConfigDict(extra="allow", frozen=True)


class {{ class_stem }}Config(FlextConfig):
    """{{ class_stem }} config auto-loaded model-less from ``config/*.yaml``."""

    {{ ns }}: _{{ ns }}Namespace = _{{ ns }}Namespace()


config: {{ class_stem }}Config = {{ class_stem }}Config.fetch_global()
"""Pre-instantiated frozen config singleton — ``from {{ package_name }} import config``."""

__all__: list[str] = ["{{ class_stem }}Config", "config"]
