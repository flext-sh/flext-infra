"""{{ class_stem }} package — eager facade (regenerate lazy exports with: make gen).

The package root freezes this facade as the stable user entrypoint. Consumers
import ``{{ class_stem }}`` or ``{{ alias }}`` from ``{{ package_name }}`` and
reach domain concerns through canonical aliases and namespaces.

Copyright (c) {{ year }} {{ author_name }}. All rights reserved.
SPDX-License-Identifier: MIT
"""

from __future__ import annotations

from {{ upstream }} import r

from {{ package_name }}._config import {{ class_stem }}Config, config
from {{ package_name }}._settings import {{ class_stem }}Settings, settings
from {{ package_name }}.api import {{ class_stem }}, {{ alias }}
from {{ package_name }}.base import {{ class_stem }}Service, s
from {{ package_name }}.constants import {{ class_stem }}Constants, c
from {{ package_name }}.models import {{ class_stem }}Models, m
from {{ package_name }}.protocols import {{ class_stem }}Protocols, p
from {{ package_name }}.typings import {{ class_stem }}Types, t
from {{ package_name }}.utilities import {{ class_stem }}Utilities, u

__all__: list[str] = [
    "{{ class_stem }}",
    "{{ class_stem }}Config",
    "{{ class_stem }}Constants",
    "{{ class_stem }}Models",
    "{{ class_stem }}Protocols",
    "{{ class_stem }}Service",
    "{{ class_stem }}Settings",
    "{{ class_stem }}Types",
    "{{ class_stem }}Utilities",
    "c",
    "config",
    "{{ alias }}",
    "m",
    "p",
    "r",
    "s",
    "settings",
    "t",
    "u",
]
