"""{{ class_stem }} package — canonical lazy public facade.

The package root freezes this facade as the stable user entrypoint. Consumers
import ``{{ class_stem }}`` or ``{{ alias }}`` from ``{{ package_name }}`` and
reach domain concerns through canonical aliases and namespaces.

Copyright (c) {{ year }} {{ author_name }}. All rights reserved.
SPDX-License-Identifier: MIT
"""

from __future__ import annotations

from typing import TYPE_CHECKING

from flext_core.lazy import build_lazy_import_map, install_lazy_exports

if TYPE_CHECKING:
    # NOTE (multi-agent, mro-j47u): all public objects stay lazy at runtime.
    from {{ upstream }} import r as r
    from {{ package_name }}._config import config as config
    from {{ package_name }}._settings import settings as settings
    from {{ package_name }}.api import (
        {{ class_stem }} as {{ class_stem }},
        {{ alias }} as {{ alias }},
    )
    from {{ package_name }}.base import (
        {{ class_stem }}Service as {{ class_stem }}Service,
        s as s,
    )
    from {{ package_name }}.constants import (
        {{ class_stem }}Constants as {{ class_stem }}Constants,
        c as c,
    )
    from {{ package_name }}.models import (
        {{ class_stem }}Models as {{ class_stem }}Models,
        m as m,
    )
    from {{ package_name }}.protocols import (
        {{ class_stem }}Protocols as {{ class_stem }}Protocols,
        p as p,
    )
    from {{ package_name }}.typings import (
        {{ class_stem }}Types as {{ class_stem }}Types,
        t as t,
    )
    from {{ package_name }}.utilities import (
        {{ class_stem }}Utilities as {{ class_stem }}Utilities,
        u as u,
    )

_LAZY_IMPORTS = build_lazy_import_map(
    {
        ".api": ("{{ class_stem }}", "{{ alias }}"),
        ".base": ("{{ class_stem }}Service", "s"),
        ".constants": ("{{ class_stem }}Constants", "c"),
        ".models": ("{{ class_stem }}Models", "m"),
        ".protocols": ("{{ class_stem }}Protocols", "p"),
        "._config": ("config",),
        "._settings": ("settings",),
        ".typings": ("{{ class_stem }}Types", "t"),
        ".utilities": ("{{ class_stem }}Utilities", "u"),
        "{{ upstream }}": ("r",),
    },
)

__all__: tuple[str, ...] = (
    "{{ class_stem }}",
    "{{ class_stem }}Constants",
    "{{ class_stem }}Models",
    "{{ class_stem }}Protocols",
    "{{ class_stem }}Service",
    "{{ class_stem }}Types",
    "{{ class_stem }}Utilities",
    "c",
    "config",
    "m",
    "p",
    "{{ alias }}",
    "r",
    "s",
    "settings",
    "t",
    "u",
)


install_lazy_exports(
    __name__,
    globals(),
    _LAZY_IMPORTS,
    public_exports=__all__,
)
