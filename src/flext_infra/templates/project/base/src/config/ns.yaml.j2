# NOTE (multi-agent, mro-wkii.19.1): namespaced U18 input validated by _models/config.py.
{{ ns }}:
  name: "{{ const_name }}"
  version: "{{ version }}"
  ping_reply: {{ scaffold.ping_example.reply | tojson }}
  disabled_reply: {{ scaffold.ping_example.disabled_reply | tojson }}
