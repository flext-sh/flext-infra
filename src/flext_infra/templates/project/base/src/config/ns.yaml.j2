# {{ class_stem }} execution config (ADR-005). Loaded model-less by _config.py.
# Add execution-parametrization keys under the `{{ ns }}:` namespace; secrets go
# in _settings.py (env-override), never here.
{{ ns }}: {}
