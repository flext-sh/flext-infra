# NOTE (multi-agent, mro-wkii.19.1): app-owned execution config is loaded from
# the project-root config directory and validated by m.{{ ns }}.Config.
# Access is config.{{ ns }}.*; environment-sourced values belong to settings.
{{ ns }}:
  greeting: "hello"
  ping_reply: "pong"
