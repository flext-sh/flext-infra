# NOTE (multi-agent, mro-wkii.19.1): namespaced U18 input validated by _models/config.py.
{{ ns }}:
  name: "{{ const_name }}"
  version: "{{ version }}"
  greeting: "hello"
  ping_reply: "pong"
  disabled_reply: "disabled"
