"""Internal config-source composition for {{ package_name }}.

Copyright (c) {{ year }} {{ author_name }}. All rights reserved.
SPDX-License-Identifier: MIT
"""

from __future__ import annotations
{# NOTE (multi-agent, mro-wkii.19.1): internal source resolution runs once and never accesses the public singleton. #}
from pathlib import Path

from {{ package_name }}._constants.config import {{ class_stem }}ConstantsConfig


class {{ class_stem }}UtilitiesConfig:
    """Resolve the packaged YAML source used once by the config model."""

    @staticmethod
    def source_directory(module_file: str) -> str:
        """Return the packaged config directory for the root config module."""
        return str(
            Path(module_file).resolve().parent
            / {{ class_stem }}ConstantsConfig.CONFIG_DIR,
        )


__all__: list[str] = ["{{ class_stem }}UtilitiesConfig"]
