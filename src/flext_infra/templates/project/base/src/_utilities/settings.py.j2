"""Internal settings-source composition for {{ package_name }}.

Copyright (c) {{ year }} {{ author_name }}. All rights reserved.
SPDX-License-Identifier: MIT
"""

from __future__ import annotations
{# NOTE (multi-agent, mro-wkii.19.1): internal source resolution runs once and never accesses the public singleton. #}
from pathlib import Path


class {{ class_stem }}UtilitiesSettings:
    """Resolve the project env file used once by the settings model."""

    @staticmethod
    def env_file(module_file: str) -> str:
        """Return the project env path for the root settings module."""
        return str(Path(module_file).resolve().parents[2] / ".env")


__all__: list[str] = ["{{ class_stem }}UtilitiesSettings"]
