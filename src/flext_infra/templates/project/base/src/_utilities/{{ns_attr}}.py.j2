"""{{ ns }} utilities namespace (composed into ``u.{{ ns }}`` via MRO).

Utilities is the ONLY facet allowed to declare helpers — pure ``@staticmethod``
functions over models/scalars. The ``utilities.py`` facade composes this class
into the ``{{ ns }}`` namespace by MRO.

Copyright (c) {{ year }} {{ author_name }}. All rights reserved.
SPDX-License-Identifier: MIT
"""

from __future__ import annotations


class {{ class_stem }}Utilities{{ ns }}:
    """{{ ns }} domain helpers (add ``@staticmethod`` helpers here)."""


__all__: list[str] = ["{{ class_stem }}Utilities{{ ns }}"]
