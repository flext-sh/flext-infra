"""{{ class_stem }} models — extends the upstream ``m`` facade (Pydantic v2).

Project-specific models live under ``m.{{ ns }}.*``; everything else is inherited
from the upstream facade. The ``{{ ns }}`` namespace is composed from the
``_models/{{ ns_attr }}.py`` domain part via MRO.

Copyright (c) {{ year }} {{ author_name }}. All rights reserved.
SPDX-License-Identifier: MIT
"""

from __future__ import annotations

from {{ upstream }} import m

from {{ package_name }}._models.{{ ns_attr }} import {{ class_stem }}Models{{ ns }}


class {{ class_stem }}Models(m):
    """{{ class_stem }} domain models extending the upstream ``m`` facade."""

    class {{ ns }}({{ class_stem }}Models{{ ns }}):
        """Project-specific models namespace (composed via MRO)."""


m = {{ class_stem }}Models

__all__: list[str] = ["{{ class_stem }}Models", "m"]
