"""{{ class_stem }} protocols — structural-typing ports under ``p.{{ ns }}.*``.

Copyright (c) {{ year }} {{ author_name }}. All rights reserved.
SPDX-License-Identifier: MIT
"""

from __future__ import annotations

from {{ upstream }} import p as _p

from {{ package_name }}._protocols.config import {{ class_stem }}ProtocolsConfig
from {{ package_name }}._protocols.settings import {{ class_stem }}ProtocolsSettings


class {{ class_stem }}Protocols(_p):
    """{{ class_stem }} protocol definitions extending the upstream ``p`` facade."""

    class {{ ns }}(
        {{ class_stem }}ProtocolsConfig,
        {{ class_stem }}ProtocolsSettings,
    ):
        """Project-specific port namespace, composed from config/settings parts via MRO."""


# NOTE (multi-agent, mro-wkii.14 / agent: codegen): facade single-base (``_p``) +
# classe interna ``{{ ns }}`` compoe *Config/*Settings via MRO (padrao cosmos-main).
p = {{ class_stem }}Protocols

__all__: list[str] = ["{{ class_stem }}Protocols", "p"]
