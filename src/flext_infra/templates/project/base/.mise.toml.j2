# Generated by `flext-infra codegen conform`.
# NOTE: Python linters and uv are resolved through the project environment.
# mise carries the compatible Python line and required non-Python binaries.

[tools]
python = "{{ python_version }}"
# Non-Python toolchain (single source: Infra.codegen.toolchain in codegen.yaml).
kubectl = "{{ kubectl_version }}"
helm = "{{ helm_version }}"
kind = "{{ kind_version }}"
gitleaks = "{{ gitleaks_version }}"
tokei = "{{ tokei_version }}"
