{
  "backend": "dolt",
  "database": "dolt",
  "dolt_mode": {{ dolt_mode | tojson }},
  "dolt_database": {{ database | tojson }}{% if project_id %},
  "project_id": {{ project_id | tojson }}{% endif %}
}
