---
name: flext-aggressive-scale-refactor
description: Apply a complete FLEXT root-cause refactor through its canonical automation.
argument-hint: Target public behavior and the observed failure.
agent: agent
---

# FLEXT root-cause refactor

Resolve authority in this order:

1. [Workspace law](../../AGENTS.md)
2. [FLEXT context routing](../../.agents/skills/flext-context-routing/SKILL.md)
3. [Branch-matched FLEXT law](../../.agents/skills/flext-law/SKILL.md)
4. Nearest scoped `AGENTS.md`
5. Active Bead

Work from observed runtime behavior and the public contract. Inventory the
owner, consumers, tests, fixtures, generated projections, documentation, and
fallbacks before mutation. Rewire every consumer to one typed SSOT, regenerate,
and remove the superseded path in the same change.

Use only selector-free root Make verbs. Mutation uses `APPLY=Y`; structural
rewires use `make mod APPLY=Y`, which owns ast-grep and standalone LSP validation.
Host-runtime CRG, LSP, Rope automation, and GitHub operations belong to ai-hub
and may augment the workflow only through public commands, hooks, MCP routes, or
daemons. Their absence is a no-op; once an available capability is selected, its
failure propagates unchanged. FLEXT never imports ai-hub as a library or declares
it as a package dependency. Git repository primitives remain owned by
`u.Infra.git_*`; GitHub provider operations remain owned by ai-hub.
Tests run only through `make test APPLY=Y` with the persistent testmon cache.

Finish only after public runtime proof, fixed-point generation, zero residue,
all declared gates, reviewed merge-commit landing, and integrated-state proof.
