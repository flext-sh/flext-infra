---
name: flext-strict-jsonvalue-session-continuation
description: Continue the active FLEXT refactor without losing its verified state.
argument-hint: Current red command, decisive output, and active scope.
agent: agent
---

# Continue the active FLEXT refactor

Reload the current intent and evidence from:

1. [Workspace law](../../AGENTS.md)
2. [FLEXT context routing](../../.agents/skills/flext-context-routing/SKILL.md)
3. [Branch-matched FLEXT law](../../.agents/skills/flext-law/SKILL.md)
4. Nearest scoped `AGENTS.md`
5. Active Bead

Resume at the first red gate. Fix its canonical owner and complete every
consumer rewire before deleting the old path. Do not introduce aliases,
compatibility, fallbacks, suppressions, local type carriers, or hardcoded
project-owned values.

Use canonical `c`, `t`, `p`, `m`, and `u` facades with Pydantic 2 boundaries.
Use `make mod APPLY=Y` for structural propagation. Use only selector-free root
Make verbs and `APPLY=Y`; every test uses `make test APPLY=Y` and testmon.

Do not report completion before fixed-point generation, public runtime proof,
all gates, zero residue, integration merge, and post-merge validation.
