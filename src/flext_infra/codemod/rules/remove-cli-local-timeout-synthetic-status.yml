id: remove-cli-local-timeout-synthetic-status
language: Python
severity: error
rule:
  pattern:
    context: |
      if True:
          pass
      elif timed_out:
          primary_exit = timeout_exit_code
    selector: elif_clause
fix: ""
message: Preserve the raw operating-system return code when a deadline expires.
