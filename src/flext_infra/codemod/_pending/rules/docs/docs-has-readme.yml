id: docs-has-readme
language: html
rule:
  any:
    - kind: text
      regex: ^#
    - kind: element
      has:
        kind: tag_name
        regex: ^h[1-6]$
message: Markdown files should have at least one heading.
severity: warning
