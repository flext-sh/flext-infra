id: rewire-cli-run-to-file-return-type
valid:
  - |
    def run_to_file(command: t.StrSequence) -> p.Result[p.Cli.ProcessOutcome]:
        return execute(command)
invalid:
  - |
    def run_to_file(command: t.StrSequence) -> p.Result[int]:
        return execute(command)
