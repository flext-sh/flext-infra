id: rewire-cli-run-to-file-return-outcome
valid:
  - "cls._execute_streamed_process(command).map(lambda output: output.outcome)"
invalid:
  - "cls._execute_streamed_process(command).map(lambda output: output.exit_code)"
