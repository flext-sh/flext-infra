id: rewire-test-monkeypatch-cwd-public-utility-import
valid:
  - |
    import pytest

    from flext_tests import u

    @pytest.fixture(autouse=True)
    def isolate_cwd(tmp_path: Path) -> Generator[None]:
        with u.Tests.Matchers.scope(cwd=tmp_path):
            yield
invalid:
  - |
    import pytest

    @pytest.fixture(autouse=True)
    def isolate_cwd(tmp_path: Path, monkeypatch: pytest.MonkeyPatch) -> None:
        monkeypatch.chdir(tmp_path)
