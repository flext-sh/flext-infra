id: rewire-cli-local-run-to-file-return-type
snapshots:
  ? |
    def run_to_file(command: str) -> p.Result[int]:
        return execute(command)
  : fixed: |+
      def run_to_file(command: str) -> p.Result[p.Cli.ProcessOutcome]:
          return execute(command)

    labels:
    - source: |-
        def run_to_file(command: str) -> p.Result[int]:
            return execute(command)
      style: primary
      start: 0
      end: 75
