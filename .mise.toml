# Generated by `flext-infra codegen conform`.
# NOTE: mise resolves compatible Python and uv lines from the config SSOT.
# Linters are resolved through the locked project environment.

[tools]
python = "prefix:3.13"
uv = "prefix:0.11"
# Deploy toolchain (single source: Infra.codegen.toolchain in codegen.yaml).
kubectl = "1.32.0"
helm = "3.19.4"
kind = "0.31.0"
taplo = "0.10.0"
