# Generated by `flext-infra codegen conform` — DO NOT EDIT.
#
# Config overrides layer (hierarchical config architecture, ADR-005):
# Layer 1: config/codegen.yaml — immutable fleet-wide business rules
# Layer 2: config/codegen-overrides.yaml — project-specific parameter overrides
#
# This layer contains ONLY per-distribution/per-project overrides that cannot
# be expressed as immutable fleet-wide rules. Every entry here is projected
# through the same typed SSOT as codegen.yaml; no raw injection.
#
# Authoritative config layers (codegen.gen.yaml):
#   primary:    config/codegen.yaml    (immutable)
#   overrides:  config/codegen-overrides.yaml (project-specific)

Infra:
  codegen:
    toolchain:
      dependency_cooldown_overrides: {}

    checkout_submodules_overrides: {}

    ci_private_submodules: {}

    make:
      custom_handler_profile_overrides:
        workspace:
          allow_public_targets: true
          allow_toolchain_declarations: true

    layout:
      project_overrides:
        flext-cli:
          archive_names:
            - settings.json
          gitignore_additions:
            - settings.json
        flext-core:
          archive_names:
            - config.json
        flext-tap-oracle-wms:
          archive_names:
            - config.json
          gitignore_additions:
            - config.json
        flext-target-oracle-oic:
          archive_names:
            - config.json
          gitignore_additions:
            - config.json
        flext-oracle-oic:
          archive_names:
            - config.json
          gitignore_additions:
            - config.json
        flext-dbt-ldif:
          moves:
            - { source: profiles/profiles.yml, target: profiles.yml }
          archive_empty_dirs:
            - profiles
        flext-dbt-oracle-wms:
          moves:
            - { source: models/reference.md, target: docs/reference.md }
          archive_empty_dirs:
            - models
        ai-hub:
          keep_root_files:
            - UNIVERSAL_CORE.md
            - ECOSYSTEM.md
            - CATALOG.md
