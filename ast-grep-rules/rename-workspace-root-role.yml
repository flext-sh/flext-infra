id: rename-workspace-role
language: Python
severity: error
message: the .gitmodules topology is named workspace, not workspace
rule:
  pattern: c.Infra.RepositoryRole.WORKSPACE_ROOT
fix: c.Infra.RepositoryRole.WORKSPACE
