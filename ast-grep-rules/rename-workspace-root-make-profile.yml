id: rename-workspace-root-make-profile
language: Python
severity: error
message: the .gitmodules topology is named workspace, not workspace-root
rule:
  pattern: c.Infra.MakeProfile.WORKSPACE_ROOT
fix: c.Infra.MakeProfile.WORKSPACE
