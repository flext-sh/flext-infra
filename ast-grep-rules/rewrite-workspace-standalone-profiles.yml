id: rewrite-workspace-standalone-profiles
language: Yaml
severity: error
message: topology profiles are workspace or standalone only
rule:
  pattern: '[workspace, standalone]'
fix: '[workspace, standalone]'
