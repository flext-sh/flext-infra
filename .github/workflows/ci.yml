# Generated by flext_infra codegen for flext-infra — DO NOT EDIT
name: CI

on:
  pull_request:
    branches:
      - 0.12.0-dev
  push:
    branches:
      - 0.12.0-dev
  workflow_dispatch:

permissions:
  contents: read

jobs:
  check:
    name: check
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.13"

      - name: Install uv
        uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          enable-cache: true

      - name: Install declared toolchain
        uses: jdx/mise-action@dba19683ed58901619b14f395a24841710cb4925 # v4.1.0

      - name: Setup (blocking)
        run: make setup

      - name: Check (blocking)
        run: make check


  test-shards:
    name: test-shard-${{ matrix.shard }}
    needs: check
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        shard: [0, 1, 2, 3, 4, 5, 6, 7]
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.13"

      - name: Install uv
        uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          enable-cache: true

      - name: Install declared toolchain
        uses: jdx/mise-action@dba19683ed58901619b14f395a24841710cb4925 # v4.1.0

      - name: Setup (blocking)
        run: make setup

      - name: Test deterministic shard (blocking)
        env:
          COVERAGE_FILE: .reports/ci-shards/.coverage.shard-${{ matrix.shard }}
          PYTEST_REPORTS_DIR: .reports/ci-shards/test-reports/shard-${{ matrix.shard }}
          PYTEST_DISABLE_PLUGIN_AUTOLOAD: "1"
        run: >-
          make test WHAT=shard
          PYTEST_ARGS="-p xdist.plugin -p pytest_cov.plugin
          -p pytest_timeout -p pytest_markdown_docs.plugin
          -p flext_tests.conftest_plugin -p flext_tests._fixtures.enforcement
          -p flext_infra.pytest_shard
          -n 4 --dist=worksteal
          --flext-shard-count 8
          --flext-shard-index ${{ matrix.shard }}
          --flext-shard-max-workers 4
          --flext-shard-assignment sha256-mod-v1
          --flext-shard-manifest
          .reports/ci-shards/shard-${{ matrix.shard }}.json"

      - name: Upload shard evidence
        if: ${{ always() }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: pytest-shard-${{ matrix.shard }}
          path: |
            .reports/ci-shards/shard-${{ matrix.shard }}.json
            .reports/ci-shards/.coverage.shard-${{ matrix.shard }}
            .reports/ci-shards/test-reports/shard-${{ matrix.shard }}/**
          if-no-files-found: error
          include-hidden-files: true

  ci:
    name: ci
    needs: test-shards
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.13"

      - name: Install uv
        uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          enable-cache: true

      - name: Install declared toolchain
        uses: jdx/mise-action@dba19683ed58901619b14f395a24841710cb4925 # v4.1.0

      - name: Setup (blocking)
        run: make setup

      - name: Download shard evidence
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: pytest-shard-*
          path: .reports/ci-shards
          merge-multiple: true

      - name: Verify exact shard union and combine coverage
        run: make test WHAT=aggregate

      - name: Upload combined test evidence
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: pytest-shard-union
          path: |
            .reports/ci-shards/coverage.xml
            .reports/ci-shards/summary.json
          if-no-files-found: error

