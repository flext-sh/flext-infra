# Generated by flext_infra codegen for flext-infra — DO NOT EDIT
name: Gate Attestation

"on":
  push:
    tags:
      - 'attest/gates/v1/**'

jobs:
  gate-attestation:
    name: gate-attestation
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
      attestations: write
    steps:
      - name: Checkout attested commit
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          fetch-depth: 0
      - name: Verify local signed receipt
        env:
          GATE_ALLOWED_SIGNERS: ${{ vars.{{ make.work_in_progress.attestation_allowed_signers_variable }} }}
          GATE_RECEIPT_OUTPUT: ${{ runner.temp }}/gate-receipt.json
        run: |
          GATE_COMMIT_SHA=$(git rev-list -n 1 "$GITHUB_REF_NAME")
          export GATE_COMMIT_SHA
          .github/scripts/gate-attestation.sh verify
      - name: Register GitHub artifact attestation
        uses: actions/attest@59d89421af93a897026c735860bf21b6eb4f7b26  # v4.1.0
        with:
          subject-path: ${{ runner.temp }}/gate-receipt.json
